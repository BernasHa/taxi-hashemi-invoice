name: taxi_invoice_app
description: Eine Flutter-App für die Generierung von PDF-Rechnungen für Taxi-Service-Tamm
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  pdf: ^3.10.7
  printing: ^5.11.1
  path_provider: ^2.1.1
  file_picker: ^6.1.1
  share_plus: ^7.2.1
  intl: ^0.18.1
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/